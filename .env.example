# ── Required ────────────────────────────────────────────
TELEGRAM_BOT_TOKEN=your_bot_token_here

# ── Authorization ───────────────────────────────────────
# Single user mode (boop-telegram compatible)
AUTHORIZED_USER_ID=your_telegram_user_id
# Multi-user mode (comma-separated, telegram-agent compatible)
AUTHORIZED_USER_IDS=
AUTHORIZED_CHAT_IDS=

# ── LLM Provider (choose one) ───────────────────────────
# Options: "anthropic" | "openai"
LLM_PROVIDER=anthropic

# Anthropic
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# OpenAI / OpenRouter
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o
OPENAI_BASE_URL=https://openrouter.ai/api/v1

# ── Convex (optional — omit for in-memory mode) ─────────
CONVEX_URL=https://your-project.convex.cloud

# ── Web Search ──────────────────────────────────────────
# Tavily (preferred) — omit to use DuckDuckGo fallback
TAVILY_API_KEY=tvly-...

# ── Bot Mode ────────────────────────────────────────────
# "polling" (default) or "webhook"
BOT_MODE=polling
# Required for webhook mode:
# PUBLIC_URL=https://your-domain.com
# BOT_PORT=3456

# ── Logging ─────────────────────────────────────────────
LOG_LEVEL=info
